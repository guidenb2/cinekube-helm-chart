apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cinekube-backend.fullname" . }}-test-connection"
  labels:
    {{- include "cinekube-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cinekube-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
