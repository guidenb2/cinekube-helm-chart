apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cinekube-movie-tracker.fullname" . }}-test-connection"
  labels:
    {{- include "cinekube-movie-tracker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cinekube-movie-tracker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
